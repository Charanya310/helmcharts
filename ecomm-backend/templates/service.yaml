apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecomm-frontend.fullname" . }}
  labels:
    {{- include "ecomm-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecomm-frontend.selectorLabels" . | nindent 4 }}
